AI_PROVIDER_PRIMARY=openai
AI_PROVIDER_SECONDARY=groq

SYSTEM_PROMPT=You are a helpful Thai tutor. Respond in Thai unless the user uses English.

# OpenAI
OPENAI_API_KEY=sk-ใส่ของจริงตรงนี้ ...
OPENAI_MODEL=gpt-4o-mini

# Groq
GROQ_API_KEY=gsk_ใส่ของจริงตรงนี้
GROQ_MODEL=llama-3.1-8b-instant

# OpenRouter (ถ้าไม่ใช้ให้เว้นว่าง)
OPENROUTER_API_KEY=or-ใส่ของจริงตรงนี้
OPENROUTER_MODEL=meta-llama/llama-3.1-8b-instruct

TEMPERATURE=0.7
MAX_TOKENS=1024
